services:
  coding-docs:
    image: nginx:1.27-alpine
    container_name: coding-docs
    ports:
      - "8002:80"
    volumes:
      # 静态产物目录：本地构建后上传 build/ 到服务器该目录即可，无需重启容器
      - ./build:/usr/share/nginx/html:ro
      - ./nginx.conf:/etc/nginx/conf.d/default.conf:ro
    restart: unless-stopped
